"""
IOlabs AI SDR Platform — Open Claw: Tool Registry (Section 13)

Tools available to the Open Claw agent:
  recon    — SerpAPI recon per candidate city, count companies matching ICP
  plan     — Build campaign plan with ranked cities and density scores
  activate — Enqueue pipeline tasks (only after human approval in DB)
  notify   — Send alerts to operator notification channels
"""

# TODO: Open Claw Phase — implement tool registry after Phase 1 completion
raise NotImplementedError("open_claw/tools.py: implemented after Phase 1 completion")
