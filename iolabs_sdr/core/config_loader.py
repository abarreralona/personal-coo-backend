"""
IOlabs AI SDR Platform — Client Configuration Loader (Step 2)

Loads and validates per-client JSON config files using Pydantic v2 models.
All client behavior is governed by these configs — nothing client-specific
is hardcoded anywhere else in the platform.

Business Rule: If a client can customize it, it lives in the config.
"""

# TODO: Step 2 — implement full Pydantic v2 models and load_client_config()
raise NotImplementedError("core/config_loader.py: implemented in Step 2")
