"""
IOlabs AI SDR Platform — Tenant / Schema Router (Step 3)

Provides per-client DB schema routing so all queries are automatically
scoped to the correct `client_{client_id}` PostgreSQL schema.
"""

# TODO: Step 3 — implement get_tenant_schema(client_id) and schema-scoped session
raise NotImplementedError("core/tenant.py: implemented in Step 3")
