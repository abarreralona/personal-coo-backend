"""
IOlabs AI SDR Platform — SQLAlchemy 2.0 Models (Step 3)

Platform schema (shared):
  platform.tenants
  platform.audit_log
  platform.campaign_results
  platform.dnc_global
  platform.seed_files
  platform.mirror_fish_runs
  platform.campaign_plans

Per-client schema (client_{client_id}.*):
  leads
  contacts
  emails_sent
  email_replies
  linkedin_messages
  dnc_list
  sequence_schedule

Full DDL spec in Section 7.
"""

# TODO: Step 3 — implement all SQLAlchemy 2.0 mapped models
raise NotImplementedError("db/models.py: implemented in Step 3")
