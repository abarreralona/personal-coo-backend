"""
IOlabs AI SDR Platform — Mirror Fish: Seed File Loader (Section 14)

Loads and parses industry seed files from platform.seed_files table.
Seed files are versioned — always load the latest approved version.

TODO: OPEN ITEM 2 — First seed file for 'packaging_distribution_midmarket'
  must be written by IOlabs operator using the format in Section 14 before
  Mirror Fish Phase 3 development can begin.
"""

# TODO: Mirror Fish Phase — implement SeedLoader.load(industry_key, version=None) after Open Items resolved
raise NotImplementedError("mirror_fish/seed_loader.py: implemented after Open Item 2 resolved")
