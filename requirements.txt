# Core API framework
fastapi==0.114.2
uvicorn[standard]==0.30.6
httpx==0.27.2

# Google API stack (OAuth + Gmail + Calendar)
google-api-python-client==2.149.0
google-auth==2.34.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# Environment variable loader
python-dotenv==1.0.1

# Data & storage
sqlite-utils==3.36  # optional, helps with schema introspection
pydantic==2.9.2     # ensures FastAPI models run properly with your version

# (Optional but recommended)
tenacity==9.0.0     # retry helper for API calls (useful for Gmail/HTTP errors)
